trigger: none

pr: none

variables:
  output: "NO_OUTPUT"
  
stages:
- stage: First
  displayName: Stage
  jobs:
  - job: First_job
    pool:
      vmImage: ubuntu-latest
    steps:
    - bash: |
        echo "Here we go"
        echo $(output)
        echo $(check_if_works)
        echo $[variables.check_if_works]

